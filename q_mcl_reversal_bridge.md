# MCL Reversal Bridge

**Quest:** recover authoritative shock reversals and mature continuations that
Stage 112 cannot admit, without loosening V18 or turning volume into direction.

**Status:** `ACTIVE / STAGES119-126 SETTLED / SHOCK-BORN RAW CONFIRMATION PROSPECTIVE / LIVE STAGE112 UNCHANGED`.

## Canonical specimen — 2026-08-06 UP leg

- Move: `74.58` low near `05:31Z` to `78.77` high near `2026-08-07 04:31Z`.
- The earlier `01:25Z` UP turn is a negative control: it preceded about `$130`
  MCL MAE and had no Stage-114 major-shock wave.
- `05:30` minute: `6.15x` volume, `5.33x` range, fully DOWN final flush.
- `05:31` minute: `14.619x` volume, `5.14x` range, price/velocity/acceleration
  UP, but the inherited 15m spine remains DOWN; Stage-112
  `strict_direction=null`, so no provisional minute shock can start.
- Seconds truth preserves the order erased by OHLC aggregation:
  `5x attention -> 10x protection -> 12.7x UP authority -> crest -> UP
  reacceleration`.
- Stage-114 events: UP bind `05:30:38`, crest `05:30:41`, continuation
  `05:30:56`, fast rotation `05:33:39`, normalization `06:28`.
- First slow confirmation after the fresh wave: all five velocities UP at
  `05:35`; 30m and 60m slopes both UP by `05:45`. A next-open bridge entry
  near `74.79` is about `0.21` above the low.

## Proven defects

1. Lowering the completed-minute onset from `15x` to `12x` is insufficient:
   the reversal minute still has `strict_direction=null`.
2. Stage 118 attaches fresh 10x+ authority to a stale ordinary phase front.
   Here an UP front begun at `03:50` had already failed; the `05:30:38` major
   wave was only an overlay and did not reset its epoch.
3. Stage 112 has no lawful mid-cycle promotion after a weak/non-admitted turn
   later matures into coherent continuation.

## Retained prospective mechanism

1. **PROTECT:** 10x+ authority blocks stale opposite fades.
2. **RESET:** every fresh Stage-114 10x+ authority bind/handoff starts a new
   phase-front epoch; it never inherits an ordinary front's failures.
3. **CONFIRM:** direction comes only from full seconds CL/MCL alignment; entry
   requires subsequent completed-5m velocity propagation and a fast slope
   cross/cascade. Volume supplies urgency, never direction.
4. **ADMIT ONCE:** schedule the existing accelerated LIMIT ladder at the next
   lawful open; no market order and no duplicate admission.
5. **OWN/HANDOFF:** Stage 112 always has position priority. A bridge may open
   only from flat, same-direction ownership attaches without a new fill, and an
   ordinary/minute-proxy bridge never interrupts an opposite Stage-112 owner.
   Every V18 raw turn, opposite higher shock, failed phase-front ownership,
   centralized loss cap, weekly flat, roll, or gap retains flatten authority.
6. **DORMANT SHOCK MEMORY:** normalization does not erase provenance. A major
   reversal epoch remains directionless-to-trading until a same-direction
   native raw confirmation arrives; any CL/MCL extreme breach, opposite
   authoritative confirmation, later major shock, roll, or gap revokes it.
   Ordinary phase fronts never inherit this permission.

## Required controls

- Rejected `01:25Z` false UP impulse.
- Quiet pivots, ordinary continuation, 5-10x subshocks, 10/12/15/20x shocks.
- Flush-reversal, direct continuation, exhaustion, recoil, handoff, and chop.
- Both directions, GTH/RTH, both available July-to-July years, roll/gap/weekend.
- Threshold-neighbor plateau; no selection from the known `74.58 -> 78.77`
  outcome.

## Crown gate

Under the explicit post-Stage-119 user amendment, beat Stage 112 on full and
most yearly PnL/DD by at least 3%, exceed its trade
count and ideally `200/year`, keep primary/stressed yearly and directional PnL
positive, reduce tails/DD, preserve exact disabled parity, and pass LMT-only,
capital, loss-cap, weekly-flat, restart, freshness, and source/runtime parity.
The immutable original Stage-119 5% preregistration/rejection remains disclosed;
the 3% law applies prospectively to this MCL successor quest and changes no
other safety, stability, cadence, or incident gate.

## Evidence pointers

- Live owners: `tradebot/research/mcl_minute_shock.py`,
  `tradebot/research/mcl_shock_arbiter.py`.
- Seconds owner: `tradebot/research/mcl_shock_crest.py`,
  `tradebot/research/mcl_shock_waves.py`.
- Phase-front contract:
  `backtests/mcl/mcl_multiscale_phase_front_stage118_preregistration.json`.
- Known-incident diagnostics: `/tmp/mcl-stage119-up-leg-owner-anatomy.json`,
  q `/var/tmp/mcl-stage119-known-up-leg-phase-front-anatomy.json`.
- Portfolio authority ledger: `q_XSP_live_research_kata.md`.

## Active boundary

Stage 119 passed exact disabled Stage-112 parity and added `$63.92` in its best
selection-year cell, but improved PnL/DD only `3.18%` versus the frozen `5%`
gate; `0/24` cells passed and locked Year 2 remained unopened. Preserve its
major-shock epoch reset and opposite-raw lifecycle, reject its two-bar fast
exit, and do not rescue it with an ATR threshold: the latter cannot supply
`200/year` cadence.

The explicit 3% amendment opened locked Year 2 once; Stage 119 failed decisively
(`+0.11% / -21.26%` yearly PnL/DD delta, `-10.04%` full, bridge
`-$370.72`). Never reopen or retune it.

Stage 120 then proved that a CL-only ordinary phase front is structurally too
broad: all eight count cells emitted `694-1,960` candidates. It was rejected
before economics because Stage 118's slow snapshot cannot stand in for
CL-to-MCL transport.

Stage 121 proved the independent matched-MCL seam at count: three connected
two-bar cells retained `181-281` balanced events versus Stage 120's
`694-1,960`. Its strict intersection then passed Year 1 spectacularly
(`+43.04%` PnL/DD; bridge `+$576.08`) but failed locked Year 2
(`-18.97%`; bridge `+$23.88` primary / `-$138.12` stressed). Full net rose to
`$4,517.51`, but DD rose to `$772.23`, reducing PnL/DD `10.44%`. Preserve the
immutable rejection; do not rescue its selected cell.

The retained mechanism insight is propagation motion, not more alignment:

- joint slope breadth `2-3` and full `5/5` lock were positive in both years;
- exactly `4/5` was a penultimate stall (`-$190.84 / -$514.44`);
- contracting joint slope-cross ETA remained positive (`+$286.40 / +$258.40`);
- fronts drifting to the scheduled source gap lost `-$291.24 / -$885.32`;
- fronts reaching a V18 raw boundary earned `+$1,012.48 / +$1,061.80`.

Any successor must therefore prove that the front is still moving: ETA must
contract, the penultimate slow-clock stall must resolve or reaccelerate, and a
post-cross fast-velocity recoil gets one bounded reacquisition window before
release. This is adaptive mechanism diagnosis after exposed outcomes; it may
enter count/prospective incubation, but cannot masquerade as new unseen crown
evidence. The authentic Stage-114 major-shock lane remains separate because
only one historical minute-proxy major survived each Stage-121 passing cell.

Stage 122 froze that motion law as one policy and rejected it economically:
full PnL/DD fell `5.56%`, Year 1 fell `2.54%`, Year 2 fell `6.42%`, and its
bridge book lost `-$172.68` primary / `-$290.68` stressed. Motion alone does
not establish authority.

Stage 123 replayed the known incident from an independent four-day CL/MCL
snapshot. The strict ordered-arrival law rejected the real winner because 60m
crossed before 30m and ETA briefly widened. Exact topology at `05:45` was
instead `UUDDD` slopes / `UDUUU` velocities on both books. Stage 124 froze
that topology without outcomes but found only `3/51` historical major epochs
(`2 DOWN / 1 UP`), so no economics opened.

Stage 125 tested the broader distinction: a phase front reaching a
same-direction raw turn with no opposing slope, at least four aligned slopes,
and `5/5` velocity on both books. Count breadth passed (`101`), but the single
frozen economic replay failed even under the amended 3% law: Year 1 PnL/DD
`-19.01%`, Year 2 `+24.21%`, full `-3.91%`, and DD `$603.63 -> $717.40`.
One `+$1,744.48` April-2026 trade supplied the apparent improvement; blanket
ordinary-front admission is permanently rejected.

The same rejected law provides a precise incident diagnostic: raw confirmation
at `06:40`, next-open long `06:41 @ 75.55`, raw-turn exit
`2026-08-07 06:21 @ 77.87`, `+$228.48` primary / `+$226.48` stressed,
`+$322` MFE and `-$64` MAE. This proves the missing capability would have
monetized most of the `$74 -> $78` leg; it does not grant live authority.

Stage 126 isolated the only causal discriminator not contradicted by the
anatomy: unbroken exceptional-volume ancestry. A frozen `5x` flush / `12x`
opposite reversal epoch could be consumed only by later same-direction Stage
125 raw confirmation while both CL and MCL retained the reversal extreme.
Across 51 historical epochs, 26 breached the extreme, 14 were superseded, six
rolled, four were revoked by opposite confirmation, and only one qualified.
Count therefore rejected before economics. Retain this exact law in the
existing prospective Stage-114/118 observer; do not dilute it or commission it
until authentic seconds episodes create a balanced cohort.

Stage 129 tested the cleanest post-raw separator from the rejected Stage-125
anatomy. Directional closes were deliberately ignored; both CL and MCL only had
to preserve the raw-turn opposite extreme for `3/4/5` completed minutes. Count
formed a genuine nested plateau: `46 -> 43 -> 41` events, every duration across
both years, directions, and GTH/RTH. The previously untested `4m` interior
medoid was frozen before delayed economics and scheduled every entry exactly
five minutes after raw confirmation. Disabled and failed-probation Stage-112
parity were both exact `388/388` (`e4d18b52...`).

The economic headline was impressive but structurally false: `398` trades,
`+$5,486.29` primary / `+$4,690.29` stressed, DD `$673.36`, PnL/DD `8.148`
(`+24.73%` versus Stage 112), and worst trade `-$333.52`. The route itself made
`+$1,050.24`, but Year 1 lost `-$221.36`, UP lost `-$251.92`, and one
`+$1,757.48` Year-2 DOWN trade supplied more than the entire edge; excluding it
left `-$707.24`. Yearly PnL/DD changed `-17.60% / +77.34%`, DD rose, and cadence
was only `199.14/year`. The explicit 3%-most-years exception therefore correctly
rejects Stage 129. Extreme survival proves non-failure, not directional or
long-lifecycle authority. Preserve it as morphology; do not rescue its duration
or weaken the stability gates.

## Current decision

### Stage 131 — observer-covered one-epoch safety veto

Friday's first authentic `20x+` control exposed a live ownership contradiction:
Stage 114 carried a `37.422x` DOWN authority wave from `17:33:33Z` to the exact
`19:20Z` V18 raw-turn boundary, yet Stage 112 matured that same raw epoch into
an UP `failed_auction` and filled `+1 MCL @ 76.93` at `19:27:03Z`. Stage 114
did not cause the order; its seconds lane is still observation-only and Stage
112's trader does not consume it.

The frozen Stage-131 contract is therefore protection-only:

- an atomic, content-addressed Stage-114 coverage watermark must prove that the
  observer evaluated through the raw-turn boundary;
- selection, contract, generation, terminal timestamp/reason/direction, raw
  event, and maturation event must match exactly;
- the matching opposite V18 `failed_auction` is vetoed once;
- proven coverage with no matching wave preserves Stage 112;
- stale or missing coverage defers the maturation and never means “no shock”;
- Stage 114 gains no direction, entry, sizing, capital, or order authority.

The prospective census is narrow: seven complete current-selection Stage-114
episodes, two raw-turn terminals, and only Friday's episode had an opposite V18
maturation. Receipt
`backtests/mcl/mcl_stage114_v18_epoch_veto_stage131_preregistration.json`
(`ed0d4955...`) freezes this boundary before implementation. Stage 130 remains
the independent count-first same-direction entry candidate and may not borrow
Stage-131 safety evidence as alpha.

The authentic coverage/race model then passed `15/15` frozen assertions. Exact
coverage vetoes; missing, lagged, or identity-mismatched coverage only defers;
HOLD never consumes the admission; proven absence releases through the existing
next-minute clock; release after `120s` expires; restart and duplicate invocation
are identity-exact; corrupt coverage fails closed; and Stage 114 cannot create a
target. Receipt
`backtests/mcl/mcl_stage114_v18_epoch_veto_stage131_coverage_race_result.json`
(`03616139...`) exposes no outcomes and submits no orders.

The safety settlement passes `10/10` further assertions. Disabled Stage 131 is
exactly the `388`-trade Stage-112 ledger (`e4d18b52...`), retaining
`+$3,942.99`, `$603.63` DD, `6.532` PnL/DD, eight loss-cap exits, and sixteen
weekly flats. Across seven authentic prospective waves it changes only the one
incident, suppressing one unique BUY and one filled MCL contract; the other
raw-turn terminal is unchanged. The position remains open, so final opportunity
cost is deliberately unsettled and no seven-episode cohort is mislabeled as a
historical alpha/tail backtest. Receipt
`backtests/mcl/mcl_stage114_v18_epoch_veto_stage131_safety_parity_settlement.json`
(`60cf92c3...`).

Live Stage 112 remains quarantined. A repaired live path requires exact
disabled `388/388` parity, incident suppression, observer lag/failure/restart
proof, honest opportunity-cost reporting, a fresh flat immutable selection,
and nontransmitting LMT commissioning.

No historical successor beats Stage 112 reliably, even at the user-approved
3% most-years hurdle. Stage 112 remains the live crown. The research branch is
closed against duration or threshold rescue. Retain `SHOCK_BORN_RAW_CONFIRMATION`
prospectively and diagnose Stage 129 only at the mechanism level: entry location,
raw-turn age, shock ancestry, post-entry velocity crest, and thesis-compatible
exit timing. No further economic replay may open inside the completed Hawkeye
block.

The first Stage-130 anatomy points to one architecture-native successor rather
than another price threshold. Every Stage-129 trade was a `failed_auction`
maturation whose frozen V18 route wanted to fade the raw direction. Fronts less
than `60m` old were unproved; fronts at least `180m` old were stale and lost
`-$1,094.48` after removing the April outlier. Fronts aged `60-180m`—exactly
`0.5x-1.5x` Stage 112's frozen `24 x 5m = 120m` state/cooldown cycle—made
`+$389.80`, PF `2.80`, `+$64.32` ex-best, Year 1/2 `+$138.40/+$251.40`,
and UP/DOWN `+$24.36/+$365.44`. The known Aug-6 pivot's repaired front was
about `170m` old. This is outcome-exposed mechanism evidence only. Queue one
count-first `NATIVE_CYCLE_RAW_CONFIRMATION` candidate; do not replay it before
the next Hawkeye `6/6` selection gate.

Stage 130 subsequently passed count breadth across neighboring
`45-165m / 60-180m / 75-195m` bands and produced impressive but formally
rejected economics in the frozen `60-180m` center: `392` trades,
`+$5,286.66`, `$555.71` DD, `9.513` PnL/DD (`+45.64%`), and positive route
books in both years and directions. It still missed the `400`-trade and
`>200/year` gates (`196.13/year`), while its largest winner supplied `53.63%`
of route gains versus the frozen `50%` ceiling. No threshold was rescued.

The post-economic known-control settlement then falsified Stage 130 as the
live incident repair. The earlier `~170m` shorthand mixed an ordinary-front
diagnostic with the exact shock epoch and is superseded:

- Aug-6 authority bound UP at `05:30:38Z` and reached raw UP at `06:40Z`,
  but topology was CL `UUU0U/UUUUU` versus MCL `UUUDU/UUUUU`. MCL's still
  opposing 240m slope violates the frozen no-opposing-slope source law, so no
  Stage-130 probation or entry exists; it also cannot meet the pre-existing
  `05:46Z` incident deadline.
- Aug-7 Stage-114 authority remained DOWN for `106.45m`, but the corresponding
  Stage-121 phase front terminated at `17:50Z` on `JOINT_RECOIL_FAILURE`, well
  before the `19:20Z` raw turn. Substituting shock-wave age for active-front age
  would be a new mechanism, so Stage 130 schedules no DOWN entry.

Receipt
`backtests/mcl/mcl_native_cycle_raw_confirmation_stage130_known_controls_rejection.json`
(`28a96669...` q result) preserves the rejection. Stage 130 remains useful
developmental morphology, not a crown, incident fix, or live owner. Stage 131
remains independently valid: it may veto the exact unsafe opposite maturation,
but it may never manufacture the desired same-direction trade. Any early
reversal or persistent-wave-memory entry successor must wait for a future
Hawkeye selection block rather than recursively rescuing this one.

## Weekend recovery freshness repair

The first transient recovery was safely disarmed before it fired. It began at
Sunday `17:58 ET` and current closed-market proof returned `type-1` but
`-1/-1`, zero-sized quotes, so it would not actually have submitted; however,
its contract did not explicitly prove positive sizes or tick freshness. The
replacement is armed for `2026-08-09 18:00:05 ET` and may submit only one
marketable `SELL 1 MCLU6 LMT` after exact `conId=661016525`, `+1` position,
zero MCL orders, live type-1 data, positive sizes, and a tick no older than
three seconds all agree. MCL live remains quarantined; SPXU, SPCX, Gold,
signals, capital, and entry authority are untouched. Receipt
`backtests/mcl/mcl_stage112_weekend_recovery_fresh_quote_repair_20260808.json`
binds script `89799993...`, focused guard proof, calendar conversion, and zero
submitted orders.

## Current deployment boundary

Stage 131 is extracted, parity-proven, pushed, and scheduled as a fresh
flat-first successor after the exact MCL-only recovery. The current predecessor
selection `8adead895...` has no Stage-131 binding and is now rejected before
generation loading, source projection, or order construction—even if the
unified systemd target is restarted. The successor requires atomic Stage-114
coverage, a flat/orderless broker, zero-fill observe-only reconciliation, and
the unchanged accelerated LMT route before either MCL timer may start.

Read-only broker truth at `2026-08-08T12:39:15Z` remains `+1 MCLU6`, `23 SPXU`,
unrelated `+1 SPCX`, and zero orders. The MCL flatten retries every `20s` until
an exact fresh type-1 NBBO permits `SELL 1 MCLU6 LMT`; the Stage-131 successor
then retries every `30s`. Stage 130 remains rejected alpha morphology and may
not be used to manufacture the missing `$74 -> $78` entry.

**Status:** `STAGE131_PROTECTION_PROVEN / PREDECESSOR_FAIL_CLOSED / EXACT_FLATTEN_AND_FRESH_SUCCESSOR_ARMED / STAGE130_REJECTED`.
