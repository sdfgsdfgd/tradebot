# MCL Leaderboard

This is the compact MCL crown registry and resume authority. Exact experimental
receipts remain content-addressed JSON; the cross-product evidence index remains
in [`q_XSP_live_research_kata.md`](../../q_XSP_live_research_kata.md). This file
keeps only the incumbent, current challenger, governing exception, closed
families, and next irreversible action.

The leaderboard separates two authorities:

- **Research crown:** an immutable historical incumbent worthy of unchanged
  prospective falsification.
- **Operational crown:** a broker-selected run that independently passes exact
  execution parity, native transport, risk, restart, and positive after-cost
  `24h → 48h → five-session` graduation.

The first does not grant the second.

---

## Crowns

| Rank | Track | Crown | Identity | State |
|---:|---|---|---|---|
| 1 | Shock-aware CL discovery → MCL transport | **MCL Shock-Aware Two-Speed Arbiter — Stage 112** | `mcl.v18-shock-stage112-executable-crown.v1` | **Executable HF research crown**; exact V18 direction plus frozen shock continuation and the mandatory live risk envelope |
| 2 | Two-speed CL discovery → MCL transport | **MCL Two-Speed Auction Relay — V18** | `mcl.two-speed-auction-relay-v18-research-crown.v1` | Preserved predecessor; its hidden magnitude inversion produced the first live loss and it is no longer selectable alone |
| — | Seconds-tight predictive timing | **Stages 87/88/89/90** | predictive generation `2256c870…` | Active challenger; autonomous `ACCUMULATE 9/30`; outcomes/order authority sealed |
| — | Authority-bound shock/subshock morphology | **Stages 114/115/116** | runtime generation `ce101ce9…`; Stage-116 preregistration `2d081df5…` | Active challenger; four immutable major-wave rows plus `21` complete subshock episodes, outcome-blind and non-trading |
| — | Multiscale phase-front morphology | **Stage 118** | preregistration `7270c1e7…`; evaluator `53cc0f20…` | `ACCUMULATE 1/30`; first fresh GTH DOWN terminal is `FAILED_FAST_OWNERSHIP`, outcome-blind and non-trading |
| — | Operational/live | **Stage 112 bounded canary** | selection `8adead89…`; plan `5dc25036…` | **CANARY_LIVE / graduation HOLD**; flat, zero inherited fills/P&L/DD, armed only for a fresh causal Stage-112 admission |

Machine pointer: `backtests/mcl/current-hf.json`, bound to immutable crown
artifact `backtests/mcl/mcl_v18_shock_stage112_executable_crown.json` at SHA-256
`c1e7aad0391a6a4ab1cd6cf2065af023d2c36058e411a464dae939599b0dd63f`.

## Resume now

| Concern | Current truth |
|---|---|
| Incumbent | **Stage 112**; exact V18 owner `mcl_two_speed_auction.py`, frozen minute-shock owner `mcl_minute_shock.py`, and sole lifecycle/risk owner `mcl_shock_arbiter.py` |
| Native historical tape | `707,136` matched CL/MCL minute rows, `25` dated pairs, every dual-volume roll, `2024-08-04 22:01Z` through `2026-08-03 20:26Z` |
| Signal identity | V18 remains byte-exact at owner SHA `03d0c958…`; the shock lane never rewrites a V18 turn and the arbiter owns all conflict, exit, cap, weekly-flat, and roll semantics |
| Lifecycle identity | **PASS:** `388/388` trades, ledger SHA `e4d18b52…`, no mismatch; arbitration plus all `8` raw-loss-cap and `16` Friday-flat events match exactly. Receipt `aedda4a3…` |
| Economics | `388` trades; `+$3,942.99` primary, `+$3,166.99` stress; PF `1.535`; DD `$603.63`; PnL/DD `6.532` |
| Stability | Year 1/year 2 `+$2,071.68 / +$1,871.31`, PnL/DD `3.496 / 3.100`; both directions and both V18/shock owners positive |
| Cadence | `194.42` annualized under the user-authorized `≥194/year` exception; one contract, no quota, no forced trade |
| Prospective tape | q generation `b3805c9f…` continuously records matched live NYMEX L1 CL/MCL seconds; independent accumulator `df806968…` joins exact V18 turns, completed multiscale context, and causal news without outcome/order/capital authority |
| Predictive challenger | Stages 87–90 hold `9/30` complete turns with outcomes sealed. Stage 114 retains four immutable authority-bound rows; Stage 115 remains a derived geometry view; Stage 116 retains `21` complete subshock episodes across both directions and sessions. Stage 117's only replay has no count authority because of neutral/missing and close-stamped-maintenance schema defects. Stage 118 receipt `bb7c3b37…` now preserves one lawful GTH DOWN front: fast ownership failed at `00:40Z`, full lock arrived only at `06:15Z`, and V18 accepted the `06:20Z` terminal. It is a negative early-onset control, not a promotion |
| Live-canary exception | **Inherited:** Stage 112 may commission as the bounded MCL canary before the seconds cohort matures; the seconds layer still has no direction/order authority and no broker/safety gate is waived |
| Live source/shadow | **PASS:** read-only finalized U6 replay through `18:43Z`, `4,003` exact common minutes, flat selection boundary, no synthetic position, zero orders; receipt `d3d5f2e4…` |
| Native commissioning | **PASS:** fresh LMT preview, gap-free successor selection `8adead89…`, current portfolio plan `5dc25036…`, predictive generation `2256c870…`, shock-wave generation `ce101ce9…`, observe-only restart, maintenance reopen, and natural recurrence are bound by the final commissioning chain |
| Alternative transport frontier | **Closed for this V18 generation:** MCO is executable but destroyed the edge; IBUSOIL 75 units was the closest near-miss but failed frozen stress PnL/DD and branch gates and lacks current CFD permission/data; OOO/UCO/SCO/USO fail bidirectional hours/economics |
| Graduation evidence | Gap-free successor cutoff `f2d2f789…` is lawful `HOLD`, not quarantine: matured-minute coverage plus runtime, identity, restart, capital, risk, and attribution pass; authentic execution and elapsed sessions remain pending. The next elapsed boundary is 48h at `2026-08-07T11:39:55Z` |
| Next irreversible evidence | Preserve the gap-free Stage-112 prefix through authentic `24h → 48h → five-session` reductions. Let Stages 87–90, 114–116, and Stage 118 accumulate unchanged from `1/30`. The one bounded Stage-118 replay is spent; do not rerun it, force a trade, backfill Stage 117, lower `12×`, or open predictive outcomes |
| Live boundary | **Flat / armed:** old selection `591db010…` remains permanently breached after `-$318.52`; gap-free Stage-112 selection `8adead89…` is current, MCL position/orders/fills are zero, and the live/predictive timers plus seconds tape are active |

## Live transport frontier

| Rank | Transport | Exact result | Decision |
|---:|---|---|---|
| 1 | Native `MCLU6`, Stage 112 | Exact `388/388` executable lifecycle parity, live type-1 source, one-contract risk envelope, LMT-only ordinary/shock chase at `1×/2×`, gap-free selection `8adead89…`, maintenance reopen, restart and natural recurrence | **Crowned; CANARY_LIVE; preliminary graduation HOLD** |
| 2 | Native `MCLU6`, V18 | First live round trip bought the crash at `$80.16`, flattened at `$76.99`, and realized `-$318.52`; selection is flat but permanently breached | **Retired from selection** |
| 3 | `IBUSOIL` CFD | `75` units: primary `+$1,468.98`, PnL/DD `3.511`; stress `+$675.69`, PnL/DD `.911`, failed-auction `-$79.75`. Live quote/preview absent because CFD permission/data is disabled | **Frozen generation rejected; future challenger only on genuinely new executable evidence** |
| 4 | `MCO` long calls/puts | Live premium/margin fits, but ATM replay was `-$1,918.94`; the only liquid symmetric ITM corridor was `-$1,428.12`; both stress cases and both directions lost | **Rejected** |
| 5 | Oil ETFs | `OOO` is ASX-hours and has no cash-buy down lane. Even perfect 24h 2× UCO/SCO tracking with zero decay/commission produced `+$967.21` gross but `-$63.41` after observed spread alone; USO has no cash-buy down companion | **Rejected** |

Receipts: `mcl_v18_native_margin_hold.json`,
`mcl_three_champion_minimum_capital_census.json`,
`mcl_ibusoil_permission_hold.json`,
`mcl_v18_ibusoil_linear_replay_rejection.json`,
`mcl_v18_mco_transport_rejection.json`, and
`mcl_oil_etf_transport_rejection.json`; Stage-112 commissioning and preliminary
graduation are bound by `mcl_v18_shock_stage112_live_commissioning_receipt.json`
(`33e1fdce…`)
and `mcl_v18_shock_stage112_preliminary_24h_graduation.json` (`e68194d8…`).
The later extended-prefix quarantine is preserved by
`mcl_v18_shock_stage112_graduation_prefix_gap_quarantine.json`; it changes no
selection, strategy, or live authority. The
retired V18 run remains frozen in `mcl_v18_live_commissioning_receipt.json`
(`8ec07209…`) and `mcl_predictive_onset_accumulator_commissioning_receipt.json`
(`12f993f0…`).

## CR-001 · 2026-08-04 · MCL Two-Speed Auction Relay — V18

**Coronation law:** the user explicitly waived the unavailable
July-2023–August-2024 dated-contract prefix for MCL research coronation. The
complete available matched native history is sufficient to name an incumbent.
The exception is MCL-specific and does not relabel missing data or waive any
prospective, runtime, broker, capital, risk, or profitability proof.

**Crown thesis:** dominant CL discovers the auction; matched-month MCL proves
whether the capital-sized transport accepts it.

1. The centralized impulse sensor reads completed `30/60/120/240/480m`
   horizons over five-minute CL bars.
2. Every raw turn immediately retains unconditional risk-reduction authority.
3. A new position waits exactly one completed five-minute maturation bar.
4. MCL lag versus CL is transport refusal and fades the raw direction.
5. Equal movement follows the raw direction.
6. MCL lead follows only when at least three raw-turn velocity horizons agree;
   otherwise it fades.
7. Failed-auction positions preserve the frozen favorable-excursion profit
   memory. Rolls reset admission and never synthesize a position.

| Slice | Trades/year | Primary | Stress | PF | Intrabar DD | PnL/DD |
|---|---:|---:|---:|---:|---:|---:|
| Discovery | `167.11` | `+$1,263.91` | `+$929.91` | `1.4108` | `$487.64` | `2.5919` |
| Validation | `171.62` | `+$1,622.58` | `+$1,280.58` | `1.4442` | `$883.52` | `1.8365` |
| Complete available tape | — | **`+$2,886.49`** | **`+$2,210.49`** | **`1.4289`** | **`$883.52`** | **`3.2670`** |

Attribution remains economically broad:

- down/up: `+$1,031.88 / +$1,854.61`;
- stressed down/up: `+$719.88 / +$1,490.61`;
- continuation/failed auction: `+$1,259.16 / +$1,627.33`;
- stressed continuation/failed auction: `+$1,175.16 / +$1,035.33`;
- directionally inverted same holds: `−$5,266.01`;
- V18 beats both frozen lead-follow/lead-fade neighbours, V13, V15,
  MCL-only direction, and fixed-roll controls.

Disclosed limitations:

- transport-acceptance was inspired by outcome-exposed anatomy; V18 is the
  prospective incumbent, not a claim of untouched unseen discovery;
- the discovery failed-auction subcohort is slightly negative at the harsher
  cost despite positive discovery total stress and positive full-tape
  failed-auction stress;
- true quote-sequence morphology cannot be reconstructed from historical
  minute bars;
- `167–172/year` satisfies the explicit `>160/year` requirement but does not
  mean one or two fills on every market day;
- live selection/capital/order authority and one authentic MCL fill now exist,
  but no closed round trip or positive profitability receipt exists yet.

## CR-002 · 2026-08-04 · Bounded live-canary exception

The user explicitly authorized V18 to enter Gold-style commissioning before the
Stage 87–89 prospective cohort matures. This waives only the wait for the fast
classifier's unseen proof. It does **not** waive exact lifecycle/runtime parity,
matched CL/MCL identity, native L1 freshness, costs, one-contract margin and
portfolio admission, two-sided non-transmitting broker preview, immutable flat
selection, restart/reconciliation/roll proof, risk reduction, or shared
`24h → 48h → five-session` graduation.

The order is immutable: first reproduce every one of the `338` frozen trades
byte-for-byte through the durable lifecycle owner; then shadow the current live
state; then commission and arm only if every downstream receipt passes. After
activation, resume Predictive Intelligence Sharpening of Onsets and causal news
alongside live graduation. Stage 87–89 may observe and later govern timing only
after its frozen cohort passes; it cannot invent direction or silently modify
the live V18 identity.

The first gate is now complete: receipt
`mcl_two_speed_auction_v18_lifecycle_owner_parity.json` at SHA-256
`05dccd2b7cf563703acd6ba6006d248779cdedee576dd4900c94b32413428bfe`
replayed `707,136` matched minutes and all 25 contract pairs. Expected and actual
ledgers contain `338` trades at the identical canonical SHA `788a5c3f…`, with
no mismatch and zero order/capital authority.

The second gate is also complete. Receipt
`mcl_v18_live_source_shadow_receipt.json` proves exact matched U6 identity and
`2,691/2,693` cache-to-broker rows; each sole mismatch is the old cache's final
partial bar, correctly replaced by the later finalized broker history. Replays
from five causal starts—including only the latest `1,946` common minutes—land
on the identical current state. The `07:05Z` raw down turn matured at `07:10Z`
but was not admitted, so the live flat state is a genuine V18 decision, not
missing warmup. Tick-by-tick-derived bars remain useful only to Stages 87–89;
receipt `mcl_tbt_direction_source_rejection.json` forbids them from silently
becoming direction truth.

## CR-003 · 2026-08-04 · Bounded live-canary commissioning

Commissioning is complete. Immutable flat selection `591db010…` / file
`61e50202…` binds V18 to dominant-discovery `CLU6` and one-contract execution
`MCLU6` under first-admitter plan `955967b2…`. The selected package freezes
fresh two-sided `PreSubmitted` LMT previews at `$0.76` commission and
`A$2,685.52/A$2,148.42` initial/maintenance margin, plus the Stage-91 stressed
loss, FX, buffer, weekly-flat, and `$300` raw-loss bounds.

Two independent cold processes reproduced source `e99ef2c7…`, transition
`06a7f481…`, zero target/position/order/fill/breach, and no inherited historical
state. The first natural five-minute invocation at `09:34:11Z` returned the same
`flat_no_target` verdict and zero submissions. `tradebot-mcl-live.timer` is
active/waiting; the service exits success between invocations. Every order is
broker-enforced LMT through the shared patient-to-relentless ladder. Receipt:
`mcl_v18_live_commissioning_receipt.json` (`8ec07209…`).

This started—not completed—operational graduation. At `11:10Z`, V18 produced a
raw down turn; its `11:15Z` one-bar maturation retained the failed-auction
reversal and admitted long. The first-admitter proof returned `ALLOW`, and the
shared ladder filled one `MCLU6` BUY LMT at `$80.16` at `11:16:19Z`, commission
`$0.76`, order ref `MCLV18-9ea40df82f8a823a48583446`. Stage 87–90 remains
non-authoritative `ACCUMULATE`; this authentic fill opens evidence, not a
profitability claim.

## CR-004 · 2026-08-04 · Shock-Aware Two-Speed Arbiter — Stage 112

Stage 112 supersedes V18 as the executable research crown. It preserves V18's
exact signal owner but gives persistent volume/range/velocity shocks their own
directional continuation lane, arbitrates both owners through one position, and
includes the same `$300` raw-loss and Friday `16:53 ET` flat laws mandatory in
live execution. Stage 110 remains the disclosed raw-alpha ceiling
(`+$4,527.99`, DD `$923.87`, PnL/DD `4.901`) but is not selectable because it
omits those mandatory protections.

| Slice | Trades | Primary | Stress | PF | Intrabar DD | PnL/DD |
|---|---:|---:|---:|---:|---:|---:|
| Year 1 | — | `+$2,071.68` | `+$1,689.68` | — | — | `3.496` |
| Year 2 | — | `+$1,871.31` | `+$1,477.31` | — | — | `3.100` |
| Complete tape | `388` | **`+$3,942.99`** | **`+$3,166.99`** | **`1.535`** | **`$603.63`** | **`6.532`** |

Against V18, Stage 112 adds `+$1,056.50`, removes `$279.89` of intrabar
drawdown, adds `50` trades, and improves PnL/DD by `99.94%`. Both directions
(`+$1,316.41 / +$2,626.58`) and both owners
(`shock +$1,771.24 / V18 +$2,171.75`) are positive. Annualized cadence is
`194.42`, accepted by the user's explicit `≥194/year` exception.

The final extraction proof replayed all `707,136` matched minutes and 25 rolls:
`388/388` trades share ledger SHA `e4d18b52…`, first mismatch is null, and the
arbiter plus all eight loss-cap and sixteen Friday-flat events match. The live
source shadow then replayed `4,003` finalized common U6 minutes through
`18:43Z`, remained flat at the successor boundary, and submitted zero orders.
Receipts: `mcl_v18_shock_stage112_executable_crown.json` (`c1e7aad0…`),
`mcl_v18_shock_stage112_durable_owner_parity.json` (`aedda4a3…`), and
`mcl_v18_shock_stage112_live_source_shadow.json` (`d3d5f2e4…`).

Today’s incident replay is diagnostic, not counted historical seconds alpha:
the prospective seconds crest would have shorted at `10:44:48 @ 81.445`, V18's
raw turn would flatten at `11:10:05 @ 80.42`, and minute-shock reacquisition
would hold `11:15:07 @ 80.2675 → 12:26 @ 77.99`; combined primary result
`+$323.21`, DD `$11.42`, versus the actual `-$318.52`. Seconds behavior remains
prospective until separately commissioned; Stage 112's crowned backtest counts
only completed-minute evidence.

## CR-005 · 2026-08-04 · Stage-112 bounded live commissioning

Fresh selection `083ecba…` atomically supersedes the breached V18 run under
portfolio plan `851714c2…`, preserving the existing XSP/Gold selections and all
three predictive-onset treatment IDs. It binds exact `CLU6/MCLU6`, one contract,
LMT-only execution, ordinary/shock AUTO speed `1×/2×`, the `$300` loss cap, and
Friday flat. Observe-only checkpoint `5769e31d…`, first natural checkpoint
`85433ec6…`, and post-deployment checkpoint `de40e0fe…` are all
`flat_no_target`, zero MCL position/order/fill/P&L/DD/breach/submission.

The cutoff-bound 24h receipt `e68194d8…` is correctly `HOLD`, not quarantine:
runtime parity, capital stability, identity, restart, risk, and attribution pass;
only authentic execution and elapsed/eligible-session evidence remain. Same-
minute receipt noise now collapses only when MCL fill, position, accounting,
drawdown, safety, order, held-direction, and submission state agree. Commissioning
receipt: `mcl_v18_shock_stage112_live_commissioning_receipt.json`
(`33e1fdce…`).

## Active challenger — Predictive Intelligence Sharpening of Onsets

V18 remains the sole direction and risk-reduction owner. One immutable live
event tape prospectively asks whether seconds-tight evidence can choose only
timing mode: `CHASE_NOW`, retain one-bar maturation, or `REJECT_FAKE`.

The frozen atlas preserves orthogonal evidence together:

- CL→MCL lead/lag, basis divergence, and re-convergence;
- microprice displacement and direction-signed slope velocity;
- velocity jerk, TR velocity/acceleration, and quote-intensity jerk;
- spread elasticity and same-price top-size pressure proxies;
- signed-print response and transport acceptance/refusal;
- completed 15m, optional same-contract 4h, and prior-week morphology;
- later causal fresh oil-news shock state without narrative direction.

Stage 88 observes `−60/−30/−15/−5/0s`; Stage 87 observes
`0/+5/+15/+30/+60s`; Stage 89 derives ordered ignition, transport refusal,
transport noise, whipsaw, exhaustion, and CL/MCL handoff. No magnitude threshold
or classifier may be fitted until the frozen cohort has at least `30` complete
raw turns, `10` each direction, `20` admissions, `5` each route, `20` resolved
handoffs, and repeated actionable shapes.

Stage 90 preregistration
`mcl_predictive_cross_scale_stage90_preregistration.json` (`db9c73c3…`)
freezes the missing fast-to-slow join before revealing either treatment's slow
morphology: exact completed 5m/15m, sparse same-contract 4h, prior finalized
five-session regime, 30m basis curvature, and causally available news by
`snapshot_as_of_utc`. Slow clocks preserve missingness and cannot veto a fast
treatment; news cannot own direction.

Stage-90 accumulation `mcl_predictive_cross_scale_stage90_accumulation.json`
(`336aa2f8…`) passes every identity/causality gate with outcomes sealed:

- `07:05Z` down: 5m path/TR and 15m velocity/acceleration align inside joint
  5m/4h expansion, but 4h slope acceleration opposes, weekly downside is
  decelerating, bullish news is stale/opposed, and first-minute handoff remains
  unresolved.
- `09:05Z` up: immediate 5m path and 15m velocity oppose inside local
  exhaustion/slow expansion, while 15m and 4h slope acceleration align, weekly
  downside is improving, fresh bullish pressure strengthens `+0.46→+0.50`, and
  CL/MCL align together at `+15→+30s`.

The second is a high-value early-torsion hypothesis seed, not an admission. A
separate q accumulator now advances the same immutable generation only after an
exact live V18 source checkpoint and complete `−60s → +5m` event window exist;
empty cycles append nothing and do not query history. Its third treatment is the
real `11:10Z` down turn that V18 routed long at `11:15Z`. The morphology records
fresh opposed `+0.50` oil-news pressure, `SAME_INTERVAL` handoff, transport
refusal, and whipsaw with `winner=null` and no exposed outcome.

The first lawful unseen prefix is now durable. Stage-88 receipt
`mcl_predictive_closing_ignition_stage88_accumulation_r2.json` (`d571f6cd…`)
contains `16,471` identity-clean seconds and two complete post-generation raw
turns: down `07:05Z` proposed failed-auction but did not admit; up `09:05Z` did
not propose. Stage-89 receipt
`mcl_predictive_velocity_jerk_stage89_accumulation.json` (`8ef53f7d…`) derives
every sign transition without an outcome, score, union, or winner. The down
control has no resolved first-minute alignment; the up control aligns CL/MCL
together only at `+15→+30s`, while its independent frozen hypotheses overlap
`ORDERED_IGNITION + TRANSPORT_REFUSAL + WHIPSAW`. That overlap is preserved,
not resolved after the fact. The successor accumulator has since preserved those
three rows byte-for-byte and appended three complete unseen abstentions once.
Status remains `ACCUMULATE`: `6/30` complete, balanced `3 down / 3 up`, `1/20`
admitted, failed-auction `1/5`, continuation `0/5`, `2/20` resolved handoffs,
zero order or timing authority. Observer ledger SHA is `363a87b7…`.

Stage 114 is the separate authority-bound shock-wave challenger. It leaves `5×`
attention directionless, binds only from full fresh CL/MCL alignment at `10×+`,
and permits an opposite handoff only at a strictly higher frozen level. Its
single runtime owner now retains four immutable rows; it remains outcome-blind
and has no timing, direction, order, or capital authority.

Stage-115 preregistration
`mcl_authority_wave_ignition_geometry_stage115_preregistration.json`
(`2c658e81…`) freezes the next derived question before the first eligible wave:
the exact ordering of `5×→10×→12×→20×` passage, CL/MCL transport alignment,
volume-flow jerk, authority bind, joint price-speed crest, continuation, handoff,
and release. It consumes only complete immutable Stage-114 episode IDs and their
existing tape/bar identities. It adds no owner, recorder, timer, ledger, score,
outcome, direction, execution, order, capital, or graduation authority. The old
V76/V77 post-entry first-adverse race remains closed.

Stage 116 reuses those exact seconds/book owners for the sub-`10×` Ignition–
Recoil–Reacceleration boundary and now preserves `21` complete episodes across
both provisional directions and both RTH/GTH. Receipt
`mcl_subshock_stage116_post_stage118_eligibility_observation.json`
(`ac2acb6a…`) freezes the first two post-Stage-118-eligibility controls: GTH UP
waves at `5.486×/7.458×`, both normalized as `MIXED_TORSION`. It remains
morphology only. Receipt
`mcl_subshock_stage116_nineteenth_episode_stage118_wait.json` adds a `6.923×`
GTH DOWN ignition/recoil/reacceleration control whose slower completed stack
subsequently resolved `UUUUU/UUUUU` without a fresh raw turn. That distinction
is why the seconds label cannot become terminal direction. Receipt
`mcl_subshock_stage116_twenty_first_episode_stage118_wait.json` closes the
formerly open `6.5×` UP wave and preserves a subsequent `5.015×` DOWN wave;
both recoiled, reacquired, and naturally normalized without opening Stage 118.

Stage 117 then spent one bounded replay asking whether shock, subshock, or
ordinary `30m` velocity origins propagate causally through the
`30/60/120/240/480m` slope stack and survive recoil. The replay was outcome-
sealed, but its evaluator mapped exact numeric `0.0` slopes to missing and
expected a nonexistent `18:00 ET` close instead of the first post-maintenance
`18:05` close. Receipt
`mcl_multiscale_phase_front_stage117_schema_rejection.json` permanently exposes
its two reported fronts and eleven anchors with no count authority.

Stage 118 is the only current phase-front successor. Preregistration
`mcl_multiscale_phase_front_stage118_preregistration.json` (`7270c1e7…`)
changes only neutral-sign semantics, close-stamped maintenance, and explicit
generation routing; hypothesis, origins, terminal law, thresholds, and count
gates are identical. Evaluator `53cc0f20…` and pre-eligibility source/turn
prefixes `d1bb50e5…/4de60a86…` were frozen before `22:45Z`. No backfill is
permitted. Receipt `mcl_multiscale_phase_front_stage118_first_derivation.json`
(`bb7c3b37…`) spends the first fresh bounded replay exactly once and advances
the cohort to `1/30`. Its ordinary DOWN front began at `00:35Z`, lost fast
ownership at `00:40Z`, crossed the 30m slope at `00:50Z`, reached full phase
lock only at `06:15Z`, and terminated on V18's accepted raw DOWN turn at
`06:20Z`. Because sponsorship failed before the first slope cross, this is
`FAILED_FAST_OWNERSHIP`: correct eventual direction, unproved early authority.
Five alternating subshock overlays do not change that classification.

## Closed search boundary

Do not reopen these historical rescue families without genuinely new data:

- naïve continuous breakout/reversion and direct XSP-fast reuse;
- fixed opening blocks, global thresholds, session/calendar rescue, and quotas;
- parity, breadth, retention, stop, target, timeout, or one-minute dwell sweeps;
- first adversity, hostile effort, reclaim/re-loss, or closing-rejection vetoes;
- generic slow takeover, phase routing, product-complex pulses, and add-on
  portfolios that failed their frozen count or economic gates;
- fabricated 4h bars, cross-roll carry, or speculative ICE/USO substitution;
- MCO ATM/ITM option dilution, the frozen IBUSOIL 50/75-unit generation, and
  OOO/UCO/SCO/USO cash-ETF transport. Reopen only on materially new executable
  evidence, never by lowering a gate after seeing the rejection.

## Operational graduation queue

1. **Done:** preserve exact V18 identity, settle the first live loss, and crown
   Stage 112 without granting it order authority.
2. **Done:** centralize the complete Stage 112 lifecycle with byte-exact
   `388/388` parity and prove the current finalized-minute source shadow.
3. **Done:** fresh funded MCLU6 identity/book and two-sided one-contract LIMIT
   what-if pass. The product-neutral first-admitter law allows each independently
   executable minimum while reserving only actual positions/pending orders plus
   the candidate at entry time; it never promises three simultaneous minima.
4. **Done:** q-owned finalized source, actual-fill risk, shared LIMIT executor,
   maintenance-aware timer, immutable CLI, shared account lock/capital law, and
   fail-closed roll/risk boundaries are deployed.
5. **Done:** fresh two-sided LMT preview and maintenance-safe succession
   published current Stage-112 selection `8adead89…`, current portfolio plan
   `5dc25036…`, and predictive generation `2256c870…` while preserving the
   active P-009 XSP and Gold selections.
6. **Done:** observe-only restart, reconciliation, flat source, zero market
   orders/submissions, and natural recurrence passed; the timer is armed and the
   new profitability clock starts from exact zero.
7. **Active:** the old gapped Stage-112 prefixes remain immutable quarantine
   evidence. The maintenance-safe successor is live and its preliminary
   reduction is `HOLD`, with every non-economic gate passing; preserve fresh
   authentic `24h → 48h → five-session` evidence without backfill.
8. **Concurrent Priority Zero:** autonomous Stages 87–90 and 114–116 continue
   accumulating outcome-blind onset/shock morphology. Stage 117 is terminally
   schema-rejected; Stage 118 is `1/30` after its sole bounded replay produced a
   failed-fast-ownership control and now accumulates only fresh future
   terminals. The predictive chain may observe Stage-112/V18 turns but never
   consume shock direction or alter the live owner.

No milestone, deadline, crown, or empty tape may force a trade.
